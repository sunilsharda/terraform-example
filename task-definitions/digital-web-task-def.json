[
  {
    "cpu": 256,
    "essential": true,
    "image": "nginx",
    "memory": 512,
    "name": "digital-web",
    "portMappings": [
      {
        "containerPort": 80,
        "hostPort": 80
      }
    ],
    "essential": true,
    "entryPoint": [],
    "command": [
    ],
    "environment": [],
    "mountPoints":[],
    "volumesFrom": [],
    "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
            "awslogs-group": "${log_group_name}",
            "awslogs-region": "${log_group_region}"
        }
    }
  }
]
